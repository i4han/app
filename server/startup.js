var Colors = new Meteor.Collection( 'colors' );
// Meteor.publish( 'colors', function() { Colors.find({}); });
var Profile = new Meteor.Collection( 'profile' );
// Meteor.publish( 'profile', function() { Colors.find({}); });
// var Connects = new Meteor.Collection( 'connects' );
// Meteor.publish( 'connects', function() { Connects.find(); });
var Boxes = new Meteor.Collection( 'boxes' );

Meteor.startup( function () {});
